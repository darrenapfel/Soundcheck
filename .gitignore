# Secrets — never commit (Soundcheck only ever needs a DEEPGRAM_API_KEY)
.env
.env.*
*.local
*.key
secrets/

# Node / build
node_modules/
dist/
build/
*.log

# Eval run artifacts (audio captures, reports) — large + regenerable
runs/
out/
*.wav
*.pcm
*.mp3
report.html

# Internal build/QA artifacts — never publish (autonomous-build briefs + review reports)
/GOAL_STS*.md
/tasks/
/docs/SOUNDCHECK_*.md

# OS / editor
.DS_Store
.vscode/
.idea/
