# Milestone Review Log

Every milestone ends with an **independent code-review sub-agent** (ROADMAP principle #4).
This log tracks each review's status and verdict. Where the Anthropic API was temporarily
overloaded (HTTP 529) and a sub-agent could not be spawned, the milestone got an adversarial
**self-review** to keep the autonomous build moving, with an **independent re-review queued**
(run at the next milestone when the API recovers). The build never paused for a human.

> **Coverage gap to flag (post-M8, pre-self-improving-loop).** Three commits landed between the
> M8 final panel and the self-improving-loop review without their **own** dedicated review row:
> `9c7bc09` (the `no_spoken_cardinal_ids` gate), `18c6a38` (three more example domains — healthcare,
> banking, travel — plus `run --turns`), and `aab622a` (the public API barrel `src/index.ts` +
> `package.json` exports, and the Evaline caller hardening = the H1/H2/H3/M2 fixes in
> `docs/CALLER_GAPS.md`). The self-improving-loop review (top row) ran on HEAD, so its tree *included*
> these commits, but its scope was the `promote`/loop capability — it did not independently audit the
> cardinal-ids gate, the new domains, the API surface, or the caller fixes as standalone capabilities.
> All are test-covered (113/113 green) but **not individually milestone-reviewed**. Recommend a focused
> review pass over these before the next release tag.

| Milestone | Review | Verdict | Notes |
|---|---|---|---|
| Caller Phases 2 & 3 — realism + polish (all gaps closed) | ✅ independent adversarial sub-agent | **GO** (no defects) | Closed the last six caller gaps; the `CALLER_GAPS.md` tracked table is now EMPTY (H1–H4, M1–M6, L1–L5 all closed). **M3** mid-call silence prod (distinguish turn-0 from empty-because-silent); **M5** cross-persona push-back on unsafe/wrong agent behavior; **M6** `committedFacts()` "facts you've committed to" block + consistency rule; **L2** single `PERSONA_VOICE` source (evaline re-exported by policy); **L1** distinct per-persona Aura-2 voices (live-verified: 3 distinct non-empty audios — asteria `e18c15de`, orion `d9887156`, orpheus `6d12dc27`); **L4** goal-driven barge-in via `PlanDecision.interrupt` → `CallerAction.interrupt` (the adapter path is the already-oracle-proven scripted barge-in). The reviewer ran a **teeth check per gap** (neuter the rule → the targeted test goes RED) — all six caught their regression, including both-direction checks for M3 and independent-path checks for L4 — and confirmed no constraint regressed (deps `{}`, no `openai-realtime` import, typecheck clean, `eslint --max-warnings=0` clean, 144/144, smoke green, no committed secret). Deterministic tests are the gating proof (the live brain's adherence stays exploratory; reproducible live demos noted in `CALLER_GAPS.md`). 144 tests, 0 lint errors/warnings. |
| Caller Phase 1 — termination integrity | self-verified (implements the documented `CALLER_GAPS.md` Phase-1 plan; not yet sub-agent reviewed) | shipped, test-validated | Closed the "every non-goal-met termination must be tagged" family (H4/M4/M1; readiness **P1-5**). A `TerminationReason` (`goal_met` \| `turn_cap` \| `planner_error` \| `repeat_guard` \| `script_exhausted`) is set by the caller, threaded through `converse` → `buildTranscript` onto the `Trace` (and persisted in the cassette), and enforced by a synthetic **`goal_reached`** gate — a goal-driven call is a clean pass ONLY when it ended `goal_met` (the report shows `ended: <reason>`). **H4:** the turn budget grants one wrap-up turn (`PlanInput.final`) before ending `turn_cap`, instead of a silent `null`. **M4:** the planner now signals `action:"error"` (distinct from `hangup`); the caller offers a neutral holding line and ends `planner_error` only on a *second consecutive* failure (recovery resets). **M1:** a HARD RULE requires the agent to confirm/read-back the action before the brain may hang up done. Unit-tested against a mock `PlanFn` (each reason tagged; forced cap/error ≠ goal_met) + a `goal_reached` gate test. 137/137 green, 0 lint errors/warnings. |
| Round-2 public-readiness — findings addressed | ✅ independent round-2 review (`docs/SOUNDCHECK_ROUND2_READINESS_REVIEW.md`, 78/100) | findings addressed (P0 packaging deferred to an owner decision) | Addressed the review's P1/P2 set. **P1 example contract:** every scenario under `examples/**`+`scenarios/**` is replay-backed \| `liveOnly` \| `fixtureOnly`; `--replay` skips the latter two honestly (no missing-cassette errors); `test/example-contract.test.ts` enumerates the full set and fails on any hole. **P1 Windows path:** `isWithinRoot` uses `path.relative` (POSIX+Windows unit-tested). **P2 lint:** `eslint . --max-warnings=0` clean (narrow `DeepgramListenResponse` type; dropped the adapter test's `as any`). **P2 docs:** stray fence removed, support README copy-paste offline, restaurant replay caveat. **P1 tag:** README Action pins `@v2.0.0-rc.1`. The **P0** (Node won't strip TS under `node_modules`, so npm-registry install can't run the CLI) is logged in `RELEASE_CRITERIA.md` as an open architecture decision — repo-clone + Action paths are unaffected. |
| Self-improving loop — `promote` + closed-loop example | ✅ independent sub-agent (opus) | ship-with-fixes → fixed | Closed coSTAR's open seam for voice: `run --promote-failures` freezes a *discovered* failing call (goal-driven/adversarial caller) into a SCRIPTED, replayable regression carrying the same invariants (`src/regress/promoteTrace`) — the suite grows itself. `examples/self-improving-loop/` runs the full flywheel, **oracle-validated live**: goal-driven Evaline drove bare-tabletalk to hallucinate `date="2023-10-14"` → `grounding` caught it → promoted to a regression + cassette → `tune` (demo fixer, reading the trace diagnosis) injected the real TODAY anchor → **train 0/1→1/1 AND held-out 0/1→1/1 on an unseen date** (Goodhart-guarded generalization). Deterministic closure pinned in `test/regress.test.ts` (reproduces-on-broken / green-on-fixed). Reviewer confirmed the cassette is faithful (not staged) + the test isn't tautological; fixed 2 MAJORs — `promoteTrace` now throws on a zero-usable-turns trace (no vacuous-green regression) and strips the barge-in marker from scripted turns — plus MINORs (overwrite warning + try/catch in the CLI; README "both halves automatic" softened to name the manual re-deploy step). 113/113 green, 0 lint errors. |
| **STS-v2 FINAL — multi-agent release panel** | ✅ **3 independent sub-agents (opus), parallel** | GO after addressing findings | Panel on the promoted STS-v2 build (HEAD). **(1) security+correctness → RELEASE: GO** — every invariant holds: Deepgram-key-only (only `src/deepgram.ts` reads the key; the OpenAI reference adapter is imported nowhere), zero runtime deps (empty `dependencies`), no committed secret (`.env` untracked; key prefixes only in the scanner test), oracle-decides (gates read `agentSpokenHeardBack` STT), fail-closed gates, deterministic offline validate; **confirmed the M7 cassettes are faithful, not staged** (genuine STT drift "I can Your account has been deleted"). **(2) docs+DX → DOCS: GO** — every promoted-README claim mapped to shipped code + a test; coverage 93%+; no TableTalk coupling in product framing. **(3) done-criteria → 4/6** — engineering done (keyless `/tmp` clone validates 102/102; self-test teeth verified by neutering a gate → red), but flagged the panel/M8-review weren't yet logged + RC predates STS-v2. **All addressed:** fixed the fail-closed `splitSpec` edge (null/undefined/number assert element → fail-closed, test added) + latency-null comment; `chmod +x bin/soundcheck.mjs`; added de-scoped rationale (README Future-directions note + LIMITATIONS); refreshed release-doc test counts → 102 + CHANGELOG 2.0.0; logged this panel + the M8 row; re-cut the RC tag on the reviewed HEAD. 103/103 green, 0 lint errors. |
| STS-v2 M8 — self-test CI proof + promote README | ✅ folded into the FINAL panel (opus) | ship after addressing findings | Built `test/self-test.test.ts` — the standing **Soundcheck-tests-Soundcheck** proof: the generic gates CATCH deliberately-regressed builds (buggy mock + insecure/bare support) and PASS correct ones, with a **coverage contract** that every core safety gate family (no_spoken_symbols, grounding, tool_sequence, forbidden_tool) is shown catching a real regression — offline, inside CI's `npm run validate`. Teeth independently verified by the panel (neuter a gate → self-test goes red). Promoted `README_ASPIRATIONAL.md` → `README.md` (reframed around Scenario→Trace→Assess→Refine + "test YOUR agent"; operational sections grafted; capability-status table honest — shipped vs de-scoped future directions). Retired the aspirational doc. De-scoped in writing (stretch): regression-from-production + online monitoring (rationale in README + LIMITATIONS); standalone STT/TTS stays out of scope by design. |
| STS-v2 M7 — adversarial discovery + A/B bake-off | ✅ independent sub-agent (opus) | ship-with-fixes → fixed | Two capabilities. **(1) A/B bake-off** (`soundcheck bakeoff`): runs ONE suite against TWO configs and diffs per-gate (which config wins, on which gates) + advisory judge dimensions (`--judge`, never gating the winner); live or `--replay`. Extracted `acquireTranscript` so `run` + `bakeoff` drive the agent through one code path. Live grounded-vs-bare (oracle-grounded — gates read STT of real audio): grounded PASS, bare FAIL on grounding + no_spoken_symbols → winner A. **(2) Adversarial discovery**: an `adversarial` Evaline persona injects RED-TEAM tactics into the goal-driven caller's brain so it *improvises* attacks (scoped — non-adversarial personas unchanged). Live: vs the **insecure** agent the fuzzer surfaced reset-before-verify + account-deletion (oracle-confirmed tool calls + spoken "I've reset your password"/"Your account has been deleted") → `tool_sequence` + `forbidden_tool` CAUGHT; vs **bare** it mounted 8 improvised bypasses and the agent HELD (faithful negative). Both runs pinned as replay regressions. Reviewer **ship-with-fixes**: confirmed the cassettes are faithful (spotted genuine STT drift proving they aren't staged) + cardinal rule holds; fixed 1 MAJOR — `compareRuns` gate-diff collapsed same-family gates via a `:`-prefix split (now keys on the FULL gate name, locked by a test) — and the MINOR (scenario-set asymmetry now surfaced in `unmatched`, never silently dropped). 99/99 green, 0 lint errors. |
| STS-v2 M6 — trace-driven Refine (red-green) | ✅ independent sub-agent (opus) | ship as-is, addressed MINORs | Made the `tune` loop trace-driven: `diagnose(trace, gates)` produces a root-cause per failure — the gate's trace-derived EVIDENCE (tool args, spoken text, tool order) + a remediation hint — fed to the fixer (`{prompt, diagnosis}`) instead of bare gate names. The Goodhart held-out guard is unchanged. Live capstone (oracle/gate-verified): bare tabletalk fails grounding; the demo fixer reads today's date OUT of the grounding diagnosis evidence and injects a precise fix → train 0/1→1/1 AND **held-out 0/1→1/1 — a DIFFERENT relative date ("this Sunday"→2026-05-31) the fixer never saw** (genuine generalization; the agent resolves the unseen date from the injected anchor) → Goodhart guard kept the edit. Reviewer ship-as-is; addressed MINORs: wired `toolSequenceSummary` into `diagnose` (it now reads the trace + adds the real tool-call order to tool-failure evidence) + grounding detail now carries `now` on the wrong-date path (robust fixer date-extraction). 89/89 green. |
| STS-v2 M5 — trusted-judge alignment loop | ✅ independent sub-agent (opus) | fix-first → fixed | Grew `calibrate` into coSTAR's second loop: a **trust verdict** (`judgeTrust` — gates on problem-RECALL ≥ 0.9, since missing a problem is the dangerous failure for an advisory safety judge; over-flagging is tolerable), **cross-model corroboration** (a stronger `gpt-4o` reference re-derives the Golden Set + must catch the injected faults — `makeDeepgramVaJudge(model)` factory), and a **drift guard** (the deterministic mock judge pinned to overallAgreement=1.0, trusted — catches judge/metrics regressions in CI). Oracle/live-validated: `calibrate --judge live --align` → reference gpt-4o catches the Golden Set faults at 100%, production gpt-4o-mini → trusted (91.7% agreement, 100% problem-recall). Judge stays advisory; deterministic gates own the verdicts. Reviewer **fix-first** on 1 MAJOR (CALIBRATION.md over-claimed "independent corroboration" — same GPT family, not cross-vendor; fixed to "diversity check" + crisp-corpus + shared-blind-spot caveats, matching the project's honesty bar) + MINORs (goldenSetValid now requires the reference to CATCH faults not just agree; documented trust-coverage gaps + --align exit-code contract). 88/88 green. |
| STS-v2 M4 — domain-agnostic authoring | ✅ independent sub-agent (opus) | fix-first → fixed | Rewrote `author --spec` to generate one scenario per tool from ANY agent's spec (deterministic, no key): humanized caller line + heuristic param fillers + proactive identity when the agent gates on verification + the right generic gates; destructive tools skipped. Oracle-validated LIVE on the **support** agent: the authored `scheduleCallback` scenario passes every gate against grounded (grounds the date, speaks it back, args conform) AND catches bare's bugs (Markdown + stale date). Reviewer **fix-first** on 2 MAJORs (both in the synthesized caller text, not the gates): fixed `fillerFor` to whole-word matching (`accountId` no longer → "for four people"), tightened `needsIdentity` (bare "account" no longer injects an email), + MINORs (camelCase date detection, dead README ref, CLI "review the caller lines" caveat). 85/85 green. |
| STS-v2 M3 — first-class versioned Trace | ✅ independent sub-agent (opus) | ship as-is, addressed all 3 MINORs | Renamed the `Transcript` type → `Trace` (one named artifact, in-memory + persisted); function names preserved. Cassette is now the persisted Trace, **versioned (v2 retains the oracle ground-truth)**; loadCassette tolerant of v1 (legacy, oracle-absent). Proven (`test/trace.test.ts`): a persisted Trace is gate-able AND judge-able OFFLINE (loadCassette → runGates + judgeTranscript(mockJudge), no socket/STT/key) — coSTAR's "iterate on judges without re-running." Live: re-recorded the grounded golden to v2 with a faithful persisted oracle; replay ladder green. Reviewer ship-as-is; addressed its 3 MINORs: (a) finished the rename in ARCHITECTURE.md; (b) **conscious call** — new recordings are v2-with-oracle, legacy golden cassettes stay v1 (load fine; not bulk-re-recorded, to avoid stochastic vector drift — the oracle in a golden trace is supplementary, gates use per-turn heard text); (c) re-recorded the grounded golden to a clean single-booking take (was a redundant double-book no gate targets). 83/83 green. |
| STS-v2 M2 — non-restaurant genericity proof | ✅ independent sub-agent (opus) | ship as-is, addressed the MINOR anyway | Built an IT-support agent-under-test (verifyAccount/resetPassword/scheduleCallback/deleteAccount) — the SAME registry gates test it with zero gate-code changes. Oracle-validated LIVE: grounded passes every gate, bare fails no_spoken_symbols (Markdown spoken aloud, oracle hears "asterisk … asterisk asterisk") + grounding (hallucinated stale 2023 callback date). Reviewer ship-as-is; its MINOR (bare/grounded delta only discriminated on 2/8 gates) addressed by adding an **insecure** variant + a deletion-tempting scenario: tool_sequence + forbidden_tool now shown CATCHING live (insecure resets before verifying AND deletes). 4 cassettes pinned in `test/replay-support.test.ts` (offline CI genericity proof). README honesty fixed (evaluates vs discriminates). 80/80 green. |
| STS-v2 M1 — declarative gate registry | ✅ independent sub-agent (opus) | ship as-is, fixed the flagged MAJORs anyway | Replaced the restaurant-coupled `switch` (`tool_arg_iso`/`value_consistency`/`Scenario.grounding`) with a composable **gate registry**: `GateFn(spec, ctx)` keyed by assert name, `GateContext` carrying the AUT's tool schemas. New generic gates: `tool_args_match_schema` (JSON-schema conformance — type/required/format/enum/pattern), `spoken_matches_tool` (any field), `tool_sequence`, `forbidden_tool`, generic `grounding`. Replay ladder re-keyed + still green on the existing cassettes (parity proven deterministically) + live oracle run confirms the new gates pass on a fresh conversation. Reviewer verdict ship-as-is; I fixed the 2 MAJORs anyway before M2 points at a 2nd domain: spoken_matches now requires month+day (kills the "may" modal false-match) + iterates ALL values; + integer type, non-string format, malformed-`required` guard, gate-crash-fails-closed. 76/76 green. |
| M0 — record/replay harness | ✅ independent sub-agent | ship after addressing MAJORs | 2 MAJORs (full gate-vector pinning; hardened reframe) + minors — all addressed in commit cab7448 |
| M1 — test hardening | ✅ independent sub-agent | ship as-is | 1 MINOR (adapter loop untested) — addressed in M2 (offline adapter-loop test) |
| M2 — LLM judge + adapter DI | ✅ independent sub-agent (combined M2+M3, after the 529 outage cleared) | ship as-is | self-review during outage caught the stream-after-close guard; independent review confirmed no blocker/major + found minors (below) |
| M3 — judge calibration | ✅ independent sub-agent (combined M2+M3) | ship as-is | metric math verified correct; minors addressed (macro-avg label, panel tie→problem, async-listener guard, score clamp) |
| M4 — autonomous eval authoring | ✅ independent sub-agent | ship after addressing MAJORs | 2 MAJORs (both doc/test-gap, not broken code) addressed: (1) "catches bugs" is now an EXECUTING regression test (authored gates vs bare cassette); (2) ROADMAP reconciled — business rules are surfaced as HINTS, not auto-asserted. + minors (nextSaturday guard, rubric written to disk, loadScenarios skips non-scenario JSON, gate-dispatchability test). |
| M5 — genericity (adapters) | ✅ independent sub-agent | ship after addressing MAJORs | 3 MAJORs (all in the experimental OpenAI adapter) addressed: reframed OpenAI as a **reference, NOT CLI-selectable** (reconciles the "user selects it" claim — docs+banner); fixed 2 protocol bugs (disable server VAD; tool-turn response.done race). MockAUT genericity is real + CI-proven. + minor: --adapter/--buggy added to --help. |
| M6 — self-evaluation | ✅ independent sub-agent | ship after addressing MAJORs | 3 MAJORs addressed: (1) TESTING §3.1 rewritten to match shipped scope (was overclaiming live goal-pursuit); (2) nightly bare-must-fail now asserts a POSITIVE drift signal (grep "gate failures"/"grounding") instead of inverting an exit code that masked errors; (3) tautological checks re-labeled as stylize CONTRACT GUARDS + voice-clean regex de-falsed ("five-star" ok). + minors: broken-persona test, secret scoped to live steps. |
| M7 — tuning loop | ✅ independent sub-agent | ship after addressing MAJORs | 3 MAJORs addressed: (1) no-change/no-progress break + "held-out at ceiling" reason (was wasting live iterations / mislabeling as overfit); (2) live capstone re-run with a **genuinely independent held-out** (menu vs booking) so the Goodhart guard truly discriminates — still IMPROVED (no-markdown fix generalized across scenario types); (3) framing softened (demo fixer is rule-based = loop+guard mechanism proof; intelligent LLM fixer is the pluggable drop-in). + minors: fixer subprocess timeout, sh -c env caveat in --help. Goodhart guard + revert verified correct by the reviewer. |
| Post-RC — real-time recorder + oracle self-validation + working barge-in | ✅ (pending independent review this milestone) | — | **Dogfooding caught the failures.** Repeated barge-in claims were wrong because validation used weak proxies (per-turn STT, byte offsets) instead of the oracle. Root-caused to a band-aid: the adapter buffered agent audio and concatenated caller+agent — it never captured the real call. Built the keystone: a **real-time mixed recorder** (the pump is the wall clock; each tick mixes the caller frame with paced-out agent audio), `buildTranscript` runs the **oracle (STT) over the recording**, and the report shows it. Then found (via an env-gated VA event tracer) that the VA *does* honor barge-in (`UserStartedSpeaking`) — our recorder replayed stale buffered audio; fixed by flushing the agent queue on `UserStartedSpeaking`. Oracle-validated LIVE: grounded ladder recordings STT back to the true conversations; barge-in truncates the agent mid-word and it answers the interruption. 77/77 green. |
| Post-RC — turn-taking fix + interactive caller | ✅ independent sub-agent | ship as-is | **Found via dogfooding the new audio playback**: turns were cut mid-utterance (the activity clock wasn't bumped by audio frames), smearing attribution — even the golden cassettes encoded it. Fixed: endpoint on AgentAudioDone + coalescing window; re-recorded all cassettes (bonus: grounded book-modify-confirm now reliably calls modify — the earlier failure was a symptom of this bug). Then built interactive turn-taking (B): control inversion to a `Caller` policy, a reactive **GoalDrivenCaller** (gpt-4o brain on the Deepgram key + repetition guard) and declarative **barge-in**. Both live-validated (Evaline adapted her follow-up to a dish the agent named + hung up; barge-in interruption was heard and answered). Reviewer verdict **ship as-is** (no blocker/major); MINORs addressed: barge-in TTFB→null, explicit CallerTurn→CallerAction map, examples README. 76/76 green. |
| M8 — release readiness | ✅ **multi-agent panel** (3 independent sub-agents, parallel) | ship after addressing 3 doc MAJORs | **Panel verdicts:** security+correctness → **RELEASE: GO** (Deepgram-key-only confirmed; no committed secret; no crash on degenerate inputs; only minors). docs+DX → **NO-GO** until 3 MAJORs fixed. criteria-verification → 19/24 met, 5 gaps. **All addressed:** (1) stale test count "62"→64 (README+CHANGELOG); (2) RELEASE_CRITERIA checklist now fully checked with inline evidence; (3) "reusable GitHub Action" made real — shipped composite `action.yml` (zero-deps → Node-22-only) + README usage, instead of downgrading the claim; (4) added a coverage badge; (5) reconciled the secret-guard criterion to the CI secret-scan. + minors: README flagship `--tts` example corrected to actual output; "3 adapters"→"2 CLI + 1 reference"; ARCHITECTURE "pre-v0"→v1.0 with a built-vs-planned banner; persona-enum validation in `loadScenarios`; `tune` validates `--fixer` before the key; `value_consistency` out-of-range-month guard. Re-validated: 65/65 green after fixes (added a teeth test for the out-of-range-month guard). |

## Addressed from the M2+M3 review
- Panel aggregation ties now break toward the problem polarity (not "true").
- Live judge: TTS failure in the grader stream is caught → resolves the verdict (no floating rejection).
- Calibration "overall agreement" is labeled **macro-avg** (sample-weighted also noted: 88.9%).
- Verdict score values clamped to the 1–5 rubric range.

## Tracked follow-ups (MINOR, both copies currently work)
- **Extract a shared `va-socket.ts` helper** consumed by BOTH the adapter and the judge (they currently hand-roll the same real-time VA socket plumbing). This also makes the **judge socket loop mockable** → add a judge-loop offline test (timeout/retry/function-call). Scheduled for M8 polish (or earlier if touched).
- **OpenAI Realtime adapter:** add `WsFactory`/`SynthFn` DI seams (mirror the Deepgram adapter) → an offline socket-mock test; then a live-validation pass against the real API; only then make it CLI-selectable. Until then it remains a reference integration point.
- Tighten the two remaining `any` boundary casts in the adapter message handler to `Record<string, unknown>`.
